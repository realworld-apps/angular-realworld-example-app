trigger:
- main

resources:
- repo: self

variables:
  # ชื่อ Image ที่จะตั้ง (แก้เป็น username ของคุณ)
  imageRepository: 'mccoycommy/angular-realworld' 
  containerRegistry: 'docker-hub-connection-v2' # ชื่อ connection ที่ตั้งใน Step 3.1
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'

  # VM Image ที่จะใช้รัน Pipeline (Ubuntu)
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      name: 'MyMacAgent' # ต้องตรงกับชื่อ Pool ที่เราสร้าง
      # vmImage: $(vmImageName)
    steps:
    # Prepare Analysis
    # - task: SonarQubePrepare@8
      #displayName: 'Prepare SonarQube Analysis'
      #inputs:
        #SonarQube: 'sonarqube-local' # ชื่อ Service Connection
        #scannerMode: 'CLI' # ใช้ CLI เพราะเราไม่ได้ build ด้วย MSBuild/Maven
        #configMode: 'manual'
        #cliProjectKey: 'Angular-Realworld' # Project Key ที่ตั้งไว้
        #cliProjectName: 'Angular-Realword'
        #cliSources: '.' # Scan ทั้ง Folder # sonar.exclusion บอกให้รู้
        #extraProperties: |
          #sonar.exclusion=**/node_modules/**,**/dist/**,**/*.spec.ts
          #sonar.typescript.lcov.reportPaths=coverage/lcov.info
          # เพิ่มบรรทัดนี้เพื่อแก้ปัญหา Branch Analysis
          #sonar.branch.name=
    # Run SonarQube Scanner (Manual Way)
    - script: |
        sonar-scanner \
          -Dsonar.projectKey=Angular-Realworld \
          -Dsonar.sources=. \
          -Dsonar.host.url=http://localhost:9000 \
          -Dsonar.login=sqa_269773f1d9ef5859bb7df3c7afa4c5f7bb7908a5 \
          -Dsonar.exclusions=**/node_modules/**,**/dist/**
      displayName: 'Run SonarQube Analysis (Manual)'
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(containerRegistry)
        tags: |
          $(tag)
          latest

    # Check whether there is trivy on OS or not.
    - script: |
        if ! command -v trivy &> /dev/null; then
          brew install trivy
        fi

    # 2. สแกน Image
    - script: |
        trivy image --exit-code 0 --severity HIGH,CRITICAL $(imageRepository):$(tag)
      displayName: 'Scan Image with Trivy' 

- stage: Deploy
  displayName: Deploy to Production
  dependsOn: Build    # ต้องรอ Build เสร็จก่อน
  condition: succeeded()
  jobs:
  - deployment: DeployWeb
    displayName: Deploy Web App
    environment: 'Production' # ชื่อต้องตรงกับที่เราสร้าง
    pool:
      name: 'MyMacAgent'
    strategy:
      runOnce:
        deploy:
          steps:
          - script: echo "🚀 Deploying to Production Server..."
            displayName: 'Simulate Deployment'